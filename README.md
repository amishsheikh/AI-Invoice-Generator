# AI-Invoice-Generator
The AI Invoice Generator is a smart web application that automatically extracts structured invoice data from unstructured text using AI. Users can paste raw invoice text containing client details, item descriptions, quantities, prices, and taxes, and the application intelligently converts it into a well-structured invoice format.
